apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "claims-chart.fullname" . }}-test-connection"
  labels:
    {{- include "claims-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "claims-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
